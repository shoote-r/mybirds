{% extends 'base.html.twig' %}

{% block title %}Garden{% endblock %}

{% block body %}
    <h1>Garden</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ garden.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ garden.description }}</td>
            </tr>
            <tr>
                <th>Size</th>
                <td>{{ garden.size }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ garden.name }}</td>
            </tr>
            <tr>
            	<th>Content</th>
            	<td><a href="{{ path('app_garden_birds', {id : garden.id})}}">All birds</a></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_member_show', {'id': garden.member.id}) }}">Back to member</a>

    <a href="{{ path('app_garden_edit', {'id': garden.id}) }}">edit</a>

    {{ include('garden/_delete_form.html.twig') }}
{% endblock %}
